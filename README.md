# koa-react-base

###Development

```bash
npm install
make dev-server
make frontend
```

###Production

```bash
make dist   # First, build for production
NODE_ENV=production node server    # then, run the production version
```
